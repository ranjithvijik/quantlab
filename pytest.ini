[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --strict-markers
    --color=yes
markers =
    unit: pure unit tests (no I/O)
    integration: end-to-end tests (may use network)
    frontend: Streamlit UI tests via AppTest
    slow: slow tests
